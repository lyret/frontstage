import { createApiClient } from "dots-wrapper";

export async function test() {
  // FIXME: continue with DNS
  //   const domain_name = "freaks.se";
  //   const connection = createApiClient({ token: DO_TOKEN });
  //   const {
  //     data: { account },
  //   } = await connection.account.getAccount();
  //   console.log(account);
  //
  //   let res = await connection.domain.listDomainRecords({ domain_name });
  //
  //   let records = res.data.domain_records.filter((record) => record.type == "A");
  //   console.log(records);
}
